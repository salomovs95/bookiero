spring:
  profiles:
    active: unittest
  jpa:
    hibernate:
      ddl-auto: create-drop
  datasource:
    url: jdbc:h2:mem:bookiero
  api:
    cors:
      allowed-origins: "*"
  security:
    secret-random: ENCODER_SUPER_SECRET
